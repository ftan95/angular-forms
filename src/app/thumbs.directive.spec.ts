import { ThumbsDirective } from './thumbs.directive';

describe('ThumbsDirective', () => {
  it('should create an instance', () => {
    const directive = new ThumbsDirective();
    expect(directive).toBeTruthy();
  });
});
